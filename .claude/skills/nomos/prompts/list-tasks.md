列出所有任务及其状态。

## 执行步骤

### 1. 读取任务映射

使用 Python 调用 TaskManager：

```python
import sys
sys.path.insert(0, '.claude/hooks')
from lib.task_manager import TaskManager

tm = TaskManager()
tasks = tm.list_tasks()
current = tm.get_current_task()
```

### 2. 按状态分组

将任务按状态分组：
- `draft`: 草稿
- `in_review`: 审阅中
- `approved`: 已批准
- `executing`: 执行中
- `done`: 已完成

### 3. 输出格式

使用 ASCII 方框图展示：

```
┌─────────────────────────────────────────────────────────────────┐
│  📋 Task List                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📍 当前任务: t1-2026-02-26-user-login                          │
│                                                                  │
│  🔵 执行中 (executing)                                          │
│  └── t1-2026-02-26-user-login    [executing]   Phase 2/3       │
│                                                                  │
│  ✅ 已完成 (done)                                               │
│  └── (无)                                                       │
│                                                                  │
│  📝 草稿 (draft)                                                │
│  └── (无)                                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4. 读取任务详情

对于每个任务，读取其 plan.md 的 YAML Frontmatter 获取：
- `status`: 任务状态
- `current_phase`: 当前阶段（如果有）

### 5. 统计信息

在底部显示统计：
- 总任务数
- 各状态任务数
- 当前活跃任务

## 示例输出

```
┌─────────────────────────────────────────────────────────────────┐
│  📋 Task List                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📍 当前任务: t1-2026-02-26-user-login                          │
│                                                                  │
│  🔵 执行中 (2)                                                  │
│  ├── t1-2026-02-26-user-login    [executing]   Phase 2/3       │
│  └── t2-2026-02-26-api-refactor  [executing]   Phase 1/2       │
│                                                                  │
│  ✅ 已完成 (1)                                                  │
│  └── t0-2026-02-25-setup         [done]                        │
│                                                                  │
│  📝 草稿 (0)                                                    │
│  └── (无)                                                       │
│                                                                  │
│  📊 统计: 总计 3 个任务                                         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```
